# ERP Agent ID Hypotheses

Черновая гипотеза по сопоставлению `agent_id` из ERP `POST /tickets/by-sell`
с каналами продаж из weekly-таблиц.

Статус:
- это не финальный mapping;
- использовать как рабочую гипотезу для сверок;
- часть соответствий подтверждена сильнее, часть пока только по паттерну сумм и косвенным признакам.

Период наблюдения:
- март 2026;
- дополнительно использовались недельные срезы `2026-03-09`, `2026-03-16`, `2026-03-23`, `2026-03-30`.
- позже добавлен контрольный weekly-срез `2026-04-20..2026-04-26`, где удалось
  сопоставить SPB `agent_id` с живой weekly-таблицей и sample-данными из ERP.

## Moscow

| agent_id | Гипотеза по каналу | Уверенность | Основание |
| --- | --- | --- | --- |
| `39992173` | `афиша.ру` | высокая | В ERP у этого `agent_id` часто встречается `client_name = "Компания Афиша"`. Недельные суммы похожи на строку `афиша.ру` в weekly-таблице. |
| `39995745` | `МДТЗК` | высокая | Прямой сигнал: в ERP найден `client_name = "МДТЗК"`. Пока единичный кейс. |
| `39320770` | `билеты виджет (сайт)` | средняя | Самый крупный поток, обычные клиентские имена, много типовых заказов. По паттерну больше всего похож на основной direct-канал сайта. |
| `39320755` | `билеты яндекс афиша` | средняя | Прямого текстового совпадения нет. Гипотеза поставлена по остаточному принципу и по соотношению недельных сумм с weekly-строкой `билеты яндекс афиша`. |
| `39996012` | `кассир` или другой внешний агрегатор | низкая/средняя | Прямого текстового подтверждения нет. Похоже на отдельный внешний агрегатор, но точное имя пока не подтверждено. |
| `0` | бесплатные / служебные / неденежные билеты | высокая | По всем наблюдениям сумма `total = 0`, при этом count заметный. Не соответствует денежным каналам weekly-таблицы. |

## Saint Petersburg

| agent_id | Гипотеза по каналу | Уверенность | Основание |
| --- | --- | --- | --- |
| `39801873` | `билеты виджет (сайт)` | высокая | В weekly-таблице за `2026-04-20..2026-04-26` строка `Сайт -> Количество заказов = 60`, и в ERP у этого `agent_id` ровно `60` paid orders. Клиентские имена обычные, без агрегаторных маркеров. |
| `39801847` | `билеты яндекс афиша` | высокая | В sample ERP-строках у всех заказов `client_email = "order@afisha.yandex.ru"`. Это самый прямой сигнал среди SPB-агентов. |
| `39996569` | `кассир` | высокая | В sample ERP-строках у заказов `client_email = "null@kassir.ru"`. |
| `39993300` | `афиша.ру (+ Т-банк)` | средняя | В ERP у заказов `client_name = "Компания Афиша"`. По смыслу это внешний афишный партнёр, не сайт и не Яндекс.Афиша. В manual weekly-таблице лучше всего ложится в bucket `афиша.ру (+Т-банк)` / `остальные агрегаторы`. |

## Working Notes

- `agent_id = 0` не нужно смешивать с денежными каналами при сверке выручки.
- Для уверенного финального mapping полезно отдельно собрать:
  - частотность `client_name` по `agent_id`;
  - частотность `agent_id` по неделям;
  - сопоставление с weekly-строками по суммам и count;
  - при наличии в ERP справочник агентов или endpoint со списком агентских каналов.

## Current Practical Mapping

Черновая рабочая версия для быстрых сверок:

```text
SPB:
39801873 -> билеты виджет (сайт)
39801847 -> билеты яндекс афиша
39993300 -> афиша.ру (+ Т-банк) / внешний афишный партнёр
39996569 -> кассир

Moscow:
39992173 -> афиша.ру
39995745 -> МДТЗК
39320770 -> билеты виджет (сайт)
39320755 -> билеты яндекс афиша
39996012 -> кассир / внешний агрегатор
0        -> бесплатные / служебные / нулевые билеты
```

## Caveat

Для Москвы последние три строки пока нельзя считать полностью подтвержденными.
Для СПб strongest matches уже такие:
- `39801873` -> site/widget;
- `39801847` -> yandex.afisha;
- `39996569` -> kassir.

Самая мягкая строка в СПб остаётся:
- `39993300` -> `афиша.ру (+ Т-банк)` / внешний афишный партнёр.

Если будем строить автоматический channel mapping в БД, лучше хранить еще и `confidence`
или отдельный флаг `is_hypothesis`.
